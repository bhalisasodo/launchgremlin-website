# LaunchGremlin Backend & MCP Server Environment Variables
PORT=5000
NODE_ENV=production

# Security & Secrets
JWT_SECRET=launchgremlin_super_secret_production_key_change_me
MCP_API_KEY=lg_mcp_prod_secure_api_key_change_me
ADMIN_PASSWORD_HASH=

# Notification Email Dispatcher
NOTIFICATION_EMAIL=bhalisasodo10@gmail.com

# Database (MongoDB Atlas connection string; leave unset for local JSON file leads.json)
MONGODB_URI=

# Outbound SMTP Credentials (for lead alert dispatch)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
